version: '3.8'

# Simplified Docker Compose for Dokploy
# Uses single container with both server and nginx
# PostgreSQL should be added as a separate database service in Dokploy

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dokploy
    environment:
      - NODE_ENV=production
      - PORT=5000
      # Database - these will be set by Dokploy's database connection
      - DB_HOST=${DB_HOST:-postgres}
      - DB_PORT=${DB_PORT:-5432}
      - DB_NAME=${DB_NAME:-mikrotik_config_builder}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
      # Security
      - JWT_SECRET=${JWT_SECRET:-your-jwt-secret-change-in-production}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-your-encryption-key-change-in-production}
      # CORS
      - CORS_ORIGIN=${CORS_ORIGIN:-http://localhost:5173}
      # Optional integrations (leave blank if not using)
      - AT_API_KEY=${AT_API_KEY:-}
      - AT_USERNAME=${AT_USERNAME:-sandbox}
      - AT_SENDER_ID=${AT_SENDER_ID:-MyISP}
      - MPESA_CONSUMER_KEY=${MPESA_CONSUMER_KEY:-}
      - MPESA_CONSUMER_SECRET=${MPESA_CONSUMER_SECRET:-}
      - MPESA_SHORTCODE=${MPESA_SHORTCODE:-174379}
      - MPESA_PASSKEY=${MPESA_PASSKEY:-}
      - WHATSAPP_ACCESS_TOKEN=${WHATSAPP_ACCESS_TOKEN:-}
      - WHATSAPP_PHONE_NUMBER_ID=${WHATSAPP_PHONE_NUMBER_ID:-}
    ports:
      - "3000:80"
    # Improved startup with better error handling
    command: >
      sh -c "
        echo '=========================================' &&
        echo 'MikroTik Config Builder - Starting...' &&
        echo '=========================================' &&
        echo 'Database: $$DB_HOST:$$DB_PORT' &&
        echo 'Waiting for database...' &&
        for i in $(seq 1 60); do
          if nc -z $$DB_HOST $$DB_PORT 2>/dev/null; then
            echo 'Database connection successful!' &&
            break
          fi
          if [ $$i -eq 60 ]; then
            echo 'ERROR: Database not available after 60 attempts' &&
            exit 1
          fi
          echo \"Waiting for database... attempt $$i/60\" &&
          sleep 2
        done &&
        echo 'Running database migrations...' &&
        cd /app/server &&
        (npm run db:migrate || echo 'WARNING: Migration failed, continuing...') &&
        (npm run db:seed || echo 'WARNING: Seed failed, continuing...') &&
        echo 'Starting application with supervisor...' &&
        exec /usr/bin/supervisord -c /etc/supervisor.d/supervisord.ini
      "
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          memory: 768M
        reservations:
          memory: 256M
